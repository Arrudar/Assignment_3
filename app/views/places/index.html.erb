<h1>Here are the places I've been</h1>

<ul>
  <% @places.each do |place| %>
    <li><%= link_to place.name, place_entries_path(place) %></li>
  <% end %>
</ul>

<h2>Add a New Place</h2>
<%= form_with model: @place, url: create_place_path, local: true do |f| %>
  <div>
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div>
    <%= f.submit "Add Place" %>
  </div>
<% end %>